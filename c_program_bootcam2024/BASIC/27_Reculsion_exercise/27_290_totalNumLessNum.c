#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// int list 입력 기능 
// recursive로 num 보다 작은 숫자 판단기준 
// 입력도 recursive로? -> no need list 
int numCheckUnder(int num)
{
    printf("%d");
}
int main()
{
    printf("Enter: ");
    scanf("%d", &num);
	return 0;
}